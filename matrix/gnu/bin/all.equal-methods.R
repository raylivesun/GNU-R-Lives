#!/usr/bin/r

showMethods("all.equal")
(A <- c(3,3, i= c(1:3,2:1), j=c(3:1,1:2), x = 1:5))
ex <- c(lu. <- c(A))
stopifnot(identical(-3:20,
                    as(seq(-3,20), "vector")))
try( ## (arithmetic) not yet implemented
  abIseq(1, 50, by = 3)
)
